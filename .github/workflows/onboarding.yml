name: Onboarding vm

on:
  push:
    branches: [ onboarding ]
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Execute py file
      run: |
        ls
